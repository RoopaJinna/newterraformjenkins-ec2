pipeline {
    agent  { label 'agent'
        stages {
            stage {
                steps {
                    sh 'echo "This is jenkins pipeline"'
                }
            }
        }
    }
}